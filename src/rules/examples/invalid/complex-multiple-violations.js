// Invalid: Complex case with multiple violations
export default function mainFunction() {
  return 'main'
}

export const helper = 'helper'
export const config = { debug: true }

export { formatDate } from './utils'

export * from './constants'